#!/bin/sh

if [[ -n "$INSIDE_EMACS" ]]; then
  EVM_EMACS="emacs"
else
  EVM_EMACS="${EMACS:-emacs}"
fi

export EVM_HOME=$(dirname $(dirname $0))

cask exec "${EVM_EMACS}" -Q --script "${EVM_HOME}/evm.el" -- "$@"
